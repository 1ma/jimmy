{
    "name": "uma/jimmy",
    "description": "Programming Bitcoin library",
    "type": "project",
    "license": "MIT",
    "autoload": {
        "psr-4": {
            "Bitcoin\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Bitcoin\\Tests\\": "tests/"
        }
    },
    "require": {
        "php": "~8.3.0",
        "ext-gmp": "*"
    },
    "require-dev": {
        "friendsofphp/php-cs-fixer": "^3.56",
        "phpunit/phpunit": "^11.1",
        "scrutinizer/ocular": "^1.9"
    },
    "scripts": {
        "lint": "@php vendor/bin/php-cs-fixer fix -v",
        "node": [
            "docker compose run --rm starter",
            "docker compose exec -T knots bitcoin-cli -regtest submitblock $(jq -r .[0] blocks.json)",
            "docker compose exec -T knots bitcoin-cli -regtest submitblock $(jq -r .[1] blocks.json)",
            "docker compose exec -T knots bitcoin-cli -regtest submitblock $(jq -r .[2] blocks.json)",
            "docker compose exec -T knots bitcoin-cli -regtest submitblock $(jq -r .[3] blocks.json)",
            "docker compose exec -T knots bitcoin-cli -regtest submitblock $(jq -r .[4] blocks.json)"
        ]
    },
    "config": {
        "sort-packages": true
    }
}
